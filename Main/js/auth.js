/**
 * Módulo de autenticación.
 * Implementación completa en la Fase 4.
 * Aquí solo viven los contratos que el resto del código ya puede importar.
 */

/** @returns {{ id: string, nombre: string, rol: string } | null} */
export function getCurrentUser() {
  // Fase 4
  return null;
}

/** @returns {'supervisor' | 'jefe_equipo' | 'jefe_zona' | null} */
export function getCurrentRol() {
  // Fase 4
  return null;
}

export async function logout() {
  // Fase 4
}
